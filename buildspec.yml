version: 1

phases:
  preBuild:
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: ./

cache:
  paths:
    - node_modules/**/*
